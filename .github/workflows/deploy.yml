name: Deploy Infra and Configure Server

on:
  push:

jobs:
  deploy:
    runs-on: ubuntu-latest
    env:
      AWS_ACCESS_KEY_ID: ${{ secrets.AWS_ACCESS_KEY_ID }}
      AWS_SECRET_ACCESS_KEY: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
      TF_VAR_public_key: ${{ secrets.SSH_PUBLIC_KEY }}
      TF_VAR_key_name: "ansible-key"

    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Set up Terraform
        uses: hashicorp/setup-terraform@v2
        with:
          terraform_wrapper: false

      - name: Terraform Init
        run: terraform init
        working-directory: terraform

      - name: Terraform Apply
        run: terraform apply -auto-approve -input=false
        working-directory: terraform

      - name: Get EC2 public IP (cleaned)
        id: get_ip
        run: |
          IP=$(terraform -chdir=terraform output -raw instance_ip)
          CLEAN_IP=$(echo "$IP" | tr -d '\r\n' | xargs)
          echo "INSTANCE_IP=$CLEAN_IP" >> $GITHUB_ENV
          echo "Cleaned IP: $CLEAN_IP"

      - name: Setup SSH key and known_hosts
        run: |
          mkdir -p ssh
          echo "${{ secrets.SSH_PRIVATE_KEY }}" > ssh/id_rsa
          chmod 600 ssh/id_rsa
          ssh-keyscan -H ${{ env.INSTANCE_IP }} > ssh/known_hosts

      - name: Create Ansible inventory
        run: |
          mkdir -p ansible/inventory
          echo "[web]" > ansible/inventory/hosts.ini
          echo "${{ env.INSTANCE_IP }} ansible_user=ubuntu ansible_ssh_private_key_file=$(pwd)/ssh/id_rsa ansible_python_interpreter=/usr/bin/python3 ansible_ssh_common_args='-o UserKnownHostsFile=$(pwd)/ssh/known_hosts -o StrictHostKeyChecking=yes'" >> ansible/inventory/hosts.ini
          cat ansible/inventory/hosts.ini

      - name: Test SSH connection
        run: ssh -i ssh/id_rsa -o UserKnownHostsFile=ssh/known_hosts -o StrictHostKeyChecking=yes ubuntu@${{ env.INSTANCE_IP }} "echo SSH connection OK"

      - name: Install Ansible
        run: sudo apt-get update && sudo apt-get install -y ansible

      - name: Run Prometheus playbook
        run: ansible-playbook ansible/playbooks/prometheus_node_exporter.yml -i ansible/inventory/hosts.ini

      - name: Run Grafana playbook
        run: ansible-playbook ansible/playbooks/grafana_prometheus.yml -i ansible/inventory/hosts.ini
